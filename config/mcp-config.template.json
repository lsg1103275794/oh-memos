{
  "mcpServers": {
    "memoslocal": {
      "command": "${BUNDLE_ROOT}/.venv/Scripts/python.exe",
      "args": ["${BUNDLE_ROOT}/mcp-server/memos_mcp_server.py"],
      "env": {
        "MEMOS_URL": "http://localhost:18000",
        "MEMOS_CUBES_DIR": "${BUNDLE_ROOT}/data/memos_cubes"
      }
    }
  }
}
